# cmd.exe misparses a batch file with Unix line endings: labels are not found
# and lines run together. These must arrive as CRLF on every checkout.
*.bat text eol=crlf
scripts/run-*.txt text eol=crlf
